import 'package:flutter/material.dart';

class PlotWeight extends StatelessWidget {
  static const routeName = '/plot';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('plot'),
    );
  }
}
